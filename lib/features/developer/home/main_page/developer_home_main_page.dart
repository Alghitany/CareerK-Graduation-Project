import 'package:flutter/material.dart';

class DeveloperHomeMainPage extends StatelessWidget {
  const DeveloperHomeMainPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
