import 'package:flutter/material.dart';

class CustomerSignUpCompulsoryData extends StatelessWidget {
  const CustomerSignUpCompulsoryData({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
